# Skills

## secure-push
Purpose: Enforce secret-safe workflow when given API keys or asked to push/commit. Keep real keys only in local `.env`/`.secrets`, use placeholders in tracked files, and never commit/push secrets.
